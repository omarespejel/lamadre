[package]
name = "lamadre"
type = "contract"
authors = ["lamadre"]
edition = "2021"

[dependencies]
# Rely on aztec-nargo from the toolchain (no external git pin)
aztec = { git = "https://github.com/AztecProtocol/aztec-packages", tag = "aztec-packages-v0.85.0", directory = "noir-projects/aztec-nr" }
